
public class Main {
	
	//getting started
    public boolean isMatch(String s, String p) {
        boolean idkyet = false;
        int token;
        //predict the future
        //if the next passage is a *
        //then skip the current section i.e (x*), where x is a variable of a char
        //check to see if the next section variable is the same as x
        //if so, then prioritise that variable over x
        for(int i = 0; i < p.length(); i++) {
            
            char a = p.charAt(i);
            // if(s.charAt(i));
            
        }
        return idkyet;
    }

	public void run() {
		
	}

}
